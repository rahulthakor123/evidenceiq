[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "truth-investigator"
version = "0.1.0"
description = "AI Knowledge and Truth Investigator"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]